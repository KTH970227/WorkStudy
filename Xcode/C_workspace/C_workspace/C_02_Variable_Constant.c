#include <stdio.h>

//변수(Variable) : 값을 담을 메모리 공간
//const : 한정자, 제어자(Qualifier)
//기호적 상수(Symbolic constant) : 변수가 값을 바꿀 수 없는
//리터널 상수(literal constant) : 문자 그대로 표현

int main()
{
    const int angel = 1004;
}
